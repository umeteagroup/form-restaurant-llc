interface:
  display_name: "Restaurant LLC Formation"
  short_description: "Form restaurant LLCs and track permits"
  default_prompt: "Use $form-restaurant-llc to collect intake, generate a formation packet, coordinate filings and permits, and maintain the closing binder."
policy:
  allow_implicit_invocation: true
